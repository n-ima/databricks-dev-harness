# 0.7.2 — 公開完了記録の書式訂正

0.7.1で採用・公開した目的別開発手順はそのまま保持する。0.7.1の公開後、完了receipt作成時に公開要件のID形式が既存parserへ適合せず拒否された。配布・更新試験の失敗ではないが、機械的な完了記録が未完了だった。

受入条件の意味は変えず、IDをPUB-01〜04へ訂正した。検証器・承認schema・権限は変更しない。再発防止として、公開手順にstamp前の既存parserとreview coverage照合を明記し、今回も旧書式の拒否・訂正後の受理・本文の同等性を実行確認する。

公開済み0.7.1を上書きせず0.7.2として固定する。案件には最新版0.7.2を指定する。0.7.0から直接取り込め、0.7.1を経由する必要はない。既存案件の保持・競合停止とUIの移行注意は[0.7.1の案内](0.7.1.md)と同じ。0.7.1を既に使っていても同じplan/apply手順で更新できる。

検証対象は旧書式の負例、既存全回帰、配布bytes/改行往復、0.7.0/0.7.1からの更新、旧版不変、独立確認とprivate main到達。結果は`work/evidence/2026-09-16-publish-072-*`と独立レビューへ記録する。実Claude Code/Copilot比較・効率改善率・実Databricks・実案件適用は未実施。GitHub CIは実際に観測した状態だけを報告する。
