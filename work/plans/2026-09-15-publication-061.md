# 0.6.1表記訂正・再検証・公開

対象taskはPUB-061、旧公開記録はSU-PUB-060。利用者の直前承認は0.6.1の再検証とprivate公開まで。今回の公開要件はdocs/harness/requirements/2026-09-15-publication-receipt-correction.md。

1. 旧4条件の文言を固定しIDだけをP060-01〜04からPUB-01〜04へ訂正する。parserを変更しない。
2. 既存contracts試験に対象文書のparse/seal確認と不正ID・条件欠落の拒否を追加し、red/greenを保存する。
3. versionと現行版案内を0.6.1に更新。別snapshotで全回帰、release生成、byte照合、独立更新試験。
4. push前に正式review JSONでsealを試行し、形式が通ることを実測する。未実行の公開項目はnot-runのまま。旧公開4条件は新reviewで意味不変を再確認する。
5. 通常push後、独立remote確認、pass receipt、task/session完了記録の追補push。

## 設計・試験の範囲

SAFE_LOCAL_UPDATEの機能設計・実装は不変。文書標準の軽微な表記変更の規定により、新規の全設計帳票・品質JSON・UIモックは作らない。旧0.6.0の品質契約は当時の証拠であり0.6.1を自動認定するものではない。今回のFIX-01〜04は差分と実行証拠を独立確認する。

未採用HARD-03のworking bytesを保存し、indexは今回の明示対象だけをstageしてexportする。実案件・Databricks・GitHub設定変更を行わない。0.6.0は不変。別タスクの0.5公開記録は今回の訂正範囲外。

候補1で全584件583成功/失敗0/skip1。公開前レビューで、FIX-04のreceipt/task閉鎖は受入条件ではなく必須の後続手順へ分離し、完了gateの自己参照を解消した。旧4条件の本文は完全不変。外部未公開の候補1を保存し、final snapshotを別pathへ生成して再検証する。
