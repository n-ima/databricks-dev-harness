# 説明と実装・証拠の整合監査

依頼: Databricks開発ハーネスについて、UI以外にも前提違反や未実装/未検証を実現済みと扱う説明がないか確認する。

これは監査であり、実装修正・正式採用・配布・実案件変更・実Databricks操作を行わない。前回UI候補の採用は保留。既存のHARD-03とUIF差分を保持する。

1. 主要求H-01〜H-20、現行README/設計/運用と版別証拠を照合する。
2. 独立contextでプラットフォーム範囲、安全/完了条件、導入/更新/記録の3群を調べる。主担当は説明の強さと証拠の射程・最新性を調べる。
3. 再現可能な問題は安全な合成fixtureで確かめ、実装を修正せず反例を保存する。
4. 各主張を「機械実装と限定検証あり」「指示/設計のみ」「実環境未検証」「説明と不一致」「未調査」に分け、日本語で報告する。

既存全試験の成功は既存試験が測った範囲だけの証拠。全過去チャットの逐語監査、全ソースの全入力、全実環境、世界中のハーネスとの比較を完了したとは扱わない。不一致を意図的虚偽と推定せず、根拠のない説明や保証は訂正する。

監査結果は `work/reviews/2026-09-16-*-truth-audit.md`。プラットフォームの現在仕様が結論に必要な時だけ公式情報を確認。調査量を品質の代用にしない。
